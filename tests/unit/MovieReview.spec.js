import { shallowMount } from "@vue/test-utils";

describe("MovieReview.vue", () => {
  it("displays the props", () => {});
  it("emits an event to delete the review when the delete review button is clicked", () => {});
});
