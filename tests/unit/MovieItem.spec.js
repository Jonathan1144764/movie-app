// import { shallowMount } from "@vue/test-utils";

// describe("MovieItem.vue", () => {
//   it("renders the props", () => {});
//   it("listens for an event to add the movie to favourites and emits an event in turn", () => {});
//   it("listens for an event to remove the movie from favourites and emits an event in turn", () => {});
//   it("listens for an event to add the movie to watchlist and emits an event in turn", () => {});
//   it("listens for an event to remove the movie from watchlist and emits an event in turn", () => {});
//   it("listens for an event to add movie rating and emits an event in turn", () => {});
//   it("emits an event to display movie details when the see details button is clicked", () => {});
// });
