// import { shallowMount } from "@vue/test-utils";

// describe("MovieList.vue", () => {
//   it("listens for an event when a movie is added to favourites and emits an event in turn", () => {});
//   it("listens for an event when a movie is removed from favourites and emits an event in turn", () => {});
//   it("listens for an event when a movie is added to watchlist and emits an event in turn", () => {});
//   it("listens for an event when a movie is removed from watchlist and emits an event in turn", () => {});
// });
