import { shallowMount } from "@vue/test-utils";

describe("MovieFavouriteList.vue", () => {
  it("emits an action to remove from favourites", () => {});
  it("emits an action to add to watchlist", () => {});
  it("emits an action to remove from watchlist", () => {});
});
