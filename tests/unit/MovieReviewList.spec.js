import { shallowMount } from "@vue/test-utils";

describe("MovieReviewList.vue", () => {
  it("shows the reviews if there are reviews associated with that movie", () => {});
  it("emits an event to delete review", () => {});
});
