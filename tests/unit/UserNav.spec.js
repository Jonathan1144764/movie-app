import { shallowMount } from "@vue/test-utils";

describe("UserNav.vue", () => {
  it("changes the number of favourites movies when movies are favourited or unfavourited", () => {});
  it("changes the number of movies in the watchlist when movies are added or removed from the watchlist", () => {});
  it("emits an event to show the favourites list when the favourites button is clicked", () => {});
  it("emits an event to show the watchlist when the watchlist button is clicked", () => {});
  it("emits an event to display all movies when the view all button is clicked", () => {});
});
